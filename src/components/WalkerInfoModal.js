import React from 'react';

const WalkerInfoModal = ({
  data
}) => {
  return (
    <div className="walkerInfoModal">
      <dialog>
        <img src="" alt="" />
        <h2>Name here</h2>

        <div className="btn-container">
          <button onClick={}>
            Accept
          </button>
          <button onClick={}>
            Decline
          </button>
        </div>
      </dialog>
    </div>
  )
}

export default WalkerInfoModal;