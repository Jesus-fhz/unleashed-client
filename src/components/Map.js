import React, { useEffect, useState } from 'react';

const Map = () => {
  const [data, setData] = useState();
  
  useEffect(() => {
    // data fetching here
  },[]);

  return (
    <div className="map">
      map here
    </div>
  )
}

export default Map;